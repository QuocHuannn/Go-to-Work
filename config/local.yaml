server:
  port: 8002
database:
  - user: admin
    password: admin
    host: localhost
    dbName: shopDEV
  - user: order
    password: order
    host: localhost
    dbName: order
security:
  jwt:
    secretKey: "yourSecretKey"
    expiresIn: "1h"